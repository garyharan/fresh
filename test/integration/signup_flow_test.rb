require "test_helper"

class SignupFlowTest < ActionDispatch::IntegrationTest
  setup do
    Rails.application.load_seed
  end

  # Current focus on mobile application.
  # This will be rewritten when we redo the web app
end
