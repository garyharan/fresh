<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/header' %>
  </head>

  <body class="text-slate-700 bg-slate-50" data-user-id="<%= authenticated? ? Current.user.id : -1 %>">

    <div class="flex flex-col">
      <%= render "shared/flashes" %>

      <main class="w-full p-0 m-0">
        <%= render "shared/nav" %>
        <%= yield %>
      </main>

    </div>

  </body>
</html>
