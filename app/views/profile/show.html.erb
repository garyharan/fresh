<% content_for(:title, "Profile") %>

<%= link_to "About", "/about", data: {
  controller: "bridge--logo",
  bridge_position: "left"
}, class: "hidden" if hotwire_native? %>

<%= render partial: 'profiles/profile', locals: { profile: @profile } %>
