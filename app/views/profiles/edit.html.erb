<% content_for(:title, "Editing profile") %>

<div class="mx-auto w-full">
  <h1 class="font-bold text-4xl hotwire-native:hidden">Editing profile</h1>
  <%= render "form", profile: @profile %>
</div>
