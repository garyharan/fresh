<div class="mx-0 w-full px-10 py-3">
  <div class="grid grid-cols-2 gap-2 md:grid-cols-1">
    <% profile.images.drop(1).each do |image| %>
      <div class="">
        <%= image_tag image.photo.variant(:medium), class: "rounded-lg shadow"  %>
      </div>
    <% end %>
  </div>
</div>
