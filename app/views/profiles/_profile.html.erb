<li>
  <%= link_to profile, class: "flex flex-col md:flex-row items-center p-2 m-2 shadow bg-white border hover:border-slate-300 rounded-lg" do %>
    <div class="flex items-center basis-1/2 grow">
      <div class="w-12 h-12 md:h-24 md:w-24">
        <%= image_tag(profile.images.first.photo.variant(:thumb), class: "w-12 h-12 md:w-24 md:h-24 rounded-full") if profile&.images&.first&.photo&.attached? %>
      </div>

      <div class="flex flex-col my-3 ml-3 text-2xl grow">
        <div>
          <%= profile.display_name %>
        </div>
        <div class="text-xl text-slate-400 grow">
          <%= "#{age(profile.born_on)}yo" unless profile.born_on.blank? %> • <%= profile.city %>
        </div>
      </div>
    </div>

    <div class="flex grow basis-1/2 justify-end items-stretch text-center">
      <div class="flex w-full place-items-center">
        <%= render partial: "profiles/details", locals: { profile: profile } %>
      </div>

      <div class="flex w-8 ml-4 hidden md:inline-block">
        <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5"></path>
        </svg>
      </div>
    </div>
  <% end %>
</li>
