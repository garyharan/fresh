<% if @profiles.empty? && Current.user&.profile == @profiles.first %>
  <% content_for :title, "Profile" %>
<% else %>
  <% content_for :title, "Discovery" %>
<% end %>

<%= render partial: "index" %>
